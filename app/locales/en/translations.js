//import locale files

import $ from 'jquery';

import MovieIndexHeader from 'app/components/movie/index-header/locale';


const appTranslations = {
    MovieIndexHeader,
};

export default $.extend({}, appTranslations);